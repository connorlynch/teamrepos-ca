<%= form_for(@retro) do |f| %>
  <% if @retro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@retro.errors.count, "error") %> prohibited this retro from being saved:</h2>

      <ul>
      <% @retro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
  <%= f.label :captured_at %>
  <%= f.date_select :captured_at, :id => "datepicker"  %>
  </div>
  
  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.collection_select :team_id, Team.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
